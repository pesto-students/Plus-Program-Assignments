// Import the custom module
const generateGreeting = require("./custom_module");

// Generate a greeting using the custom module
const greeting = generateGreeting("John");

// Display the greeting
console.log(greeting);
